import {sql} from "drizzle-orm";import {integer,sqliteTable,text} from "drizzle-orm/sqlite-core";
export const waitlistEntries=sqliteTable("waitlist_entries",{id:integer("id").primaryKey({autoIncrement:true}),name:text("name").notNull(),email:text("email").notNull().unique(),interest:text("interest"),message:text("message"),createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`)});
export const events=sqliteTable("events",{id:integer("id").primaryKey({autoIncrement:true}),quincunxPillar:text("quincunx_pillar",{enum:["ignition","grounding","release","ascension","union"]}).notNull(),dodecanicHouse:integer("dodecanic_house").notNull()});
export const sessionOfferings=sqliteTable("session_offerings",{id:integer("id").primaryKey({autoIncrement:true}),quincunxPillar:text("quincunx_pillar",{enum:["ignition","grounding","release","ascension","union"]}).notNull(),dodecanicHouse:integer("dodecanic_house").notNull()});
export const gallerySubmissions=sqliteTable("gallery_submissions",{id:integer("id").primaryKey({autoIncrement:true}),eventId:integer("event_id").references(()=>events.id),submitterName:text("submitter_name").notNull(),submitterEmail:text("submitter_email").notNull(),photoUrl:text("photo_url").notNull(),caption:text("caption"),consentSubjects:integer("consent_subjects",{mode:"boolean"}).notNull().default(false),isApproved:integer("is_approved",{mode:"boolean"}).notNull().default(false),createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`)});
