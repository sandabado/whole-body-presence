"use client";import {useState,type FormEvent} from "react";export function WaitlistForm(){const[state,setState]=useState<"idle"|"loading"|"sent"|"error">("idle");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setState("loading");const data=Object.fromEntries(new FormData(e.currentTarget));const res=await fetch("/api/waitlist",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});setState(res.ok?"sent":"error")}if(state==="sent")return <div className="success"><span>🜂</span><h2>Your name is in the fire.</h2><p>We’ll write when the first circle opens.</p></div>;return <form className="waitlist-form" onSubmit={submit}><label>YOUR NAME<input name="name" required/></label><label>EMAIL<input name="email" type="email" required/></label><label>WHAT CALLS TO YOU?<select name="interest"><option>Retreats</option><option>Private sessions</option><option>Community gatherings</option><option>Practitioner circle</option></select></label><label>ANYTHING WE SHOULD KNOW?<textarea name="message" rows={4}/></label><input className="honeypot" name="_gotcha" tabIndex={-1}/><button className="button ember" disabled={state==="loading"}>{state==="loading"?"ADDING YOUR NAME…":"ADD MY NAME TO THE FIRE →"}</button>{state==="error"&&<p>Something interrupted the signal. Please try again.</p>}</form>}
