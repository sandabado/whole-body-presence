import Link from "next/link";import {events,practitioners} from "./data";
export function PractitionerCard({person}:{person:(typeof practitioners)[number]}){return <Link href={`/practitioners/${person.slug}`} className="person-card"><div className="image-wrap"><img src={person.image} alt={person.name}/><span>FROM {person.price}</span></div><div className="card-copy"><h3>{person.name}</h3><small>{person.title}</small><p>{person.bio}</p><div className="tags">{person.tags.map(t=><b key={t}>{t}</b>)}</div><em>📍 {person.location}</em><strong>VIEW PROFILE →</strong></div></Link>}
export function EventCard({event}:{event:(typeof events)[number]}){return <Link href={`/events/${event.slug}`} className="event-card"><div className="image-wrap"><img src={event.image} alt={event.title}/><span>{event.kind}</span></div><div className="card-copy"><small>{event.date}</small><h3>{event.title}</h3><p>{event.description}</p><dl><div><dt>PLACE</dt><dd>{event.location}</dd></div><div><dt>FEE</dt><dd>{event.price}</dd></div></dl><strong>ENTER THE GATHERING →</strong></div></Link>}
