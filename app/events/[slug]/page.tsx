import {notFound} from "next/navigation";import {events} from "../../components/data";export function generateStaticParams(){return events.map(e=>({slug:e.slug}))}export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const e=events.find(x=>x.slug===slug);if(!e)notFound();return <div className="page"><section className="detail-hero"><img src={e.image} alt={e.title}/><div/><span><p className="eyebrow">{e.date}</p><h1>{e.title}</h1><p>{e.location}</p></span></section><section className="event-detail"><div><p className="eyebrow">THE EXPERIENCE</p><h2>When the desert speaks, you listen.</h2><p>{e.description}</p><p>This is not a vacation and not a performance. It is a deliberate step into fire. You will be seen, held, and given room to integrate what changes.</p><h3>THE RHYTHM</h3>{["Arrival, ground, settle","Opening circle + intention setting","Sunrise somatic practice","Breathwork intensive","Nature walk + solo time","Fire ceremony","Integration circle","Closing meal"].map((x,i)=><p className="schedule" key={x}><b>{String(i+1).padStart(2,"0")}</b>{x}</p>)}</div><aside><p className="eyebrow">INVESTMENT</p><strong>{e.price}</strong><small>SCHOLARSHIP PLACES AVAILABLE</small><dl><div><dt>DATE</dt><dd>{e.date}</dd></div><div><dt>PLACE</dt><dd>{e.location}</dd></div><div><dt>OPEN</dt><dd>{e.spots} spots</dd></div></dl><a className="button ember" href="/waitlist">RESERVE YOUR SPOT →</a><a className="button" href="/contact">ASK A QUESTION</a></aside></section></div>}
