import {notFound} from "next/navigation";import {practitioners} from "../../components/data";export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=practitioners.find(x=>x.slug===slug);if(!p)notFound();return <div className="page"><section className="booking"><div><img src={p.image} alt={p.name}/><p className="eyebrow">PRIVATE SESSION</p><h1>Work with {p.name}.</h1><p>{p.bio}</p><blockquote>Secure virtual sessions are held through Proton Meet. The link opens only during your scheduled room.</blockquote></div><form><label>FULL NAME<input required placeholder="Who are you?"/></label><label>EMAIL<input type="email" required placeholder="Where should we write?"/></label><label>SESSION LENGTH<select><option>30 minutes · {p.price}</option><option>60 minutes · $250</option><option>90 minutes · $350</option></select></label><label>WHAT WOULD YOU LIKE TO WORK ON?<textarea rows={5}/></label><label className="check"><input type="checkbox" required/> I agree to the code of conduct and consent to being contacted.</label><button className="button ember">REQUEST A SESSION →</button><small>Booking opens with the invite-only beta in December 2026.</small></form></section></div>}
