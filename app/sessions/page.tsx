import Link from "next/link";import {practitioners} from "../components/data";export default function Page(){return <div className="page"><header className="page-hero"><p className="eyebrow">PRIVATE SESSIONS</p><h1>One room. Two people. No performance.</h1><p>Somatic work, breath, ceremony preparation, coaching, and integration—in person or through encrypted Proton Meet.</p></header><section className="session-grid section">{practitioners.slice(0,4).map(p=><article key={p.slug}><img src={p.image} alt={p.name}/><div><small>{p.title}</small><h2>{p.name}</h2><p>{p.bio}</p><b>FROM {p.price}</b><Link className="button ember" href={`/sessions/${p.slug}`}>SEE AVAILABILITY →</Link></div></article>)}</section></div>}
